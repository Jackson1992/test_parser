language: cpp
compiler: gcc
script:
 - sudo apt-get update
 - cd /usr/lib
 - ls
 - sudo apt-cache search libxs-dev
 - ./build.sh init
 - pwd
 - ./configure --prefix=/home/travis/build/Jackson1992/test_parser/install 
 - sudo make 
 - sudo make install
